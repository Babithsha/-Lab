import { createMockModel } from './local-db';

export const User = createMockModel('User', {});
export const Equipment = createMockModel('Equipment', {});
export const Booking = createMockModel('Booking', {});
export const Announcement = createMockModel('Announcement', {});
export const Lab = createMockModel('Lab', {});
